#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)

	Если  Не ЗначениеЗаполнено(НаборКонстант.СтрокаПодключенияIP) Тогда
		ВызватьИсключение "Нет IP адреса для обмена. Введите адрес!";
	КонецЕсли;

	Если Не ОбщегоНазначенияВызовСервера.ПриложениеЗарегистрировано() Тогда
		ОткрытьФорму("ОбщаяФорма.РегистрацияУстройства");
	КонецЕсли;

КонецПроцедуры

#КонецОбласти